@model System.Web.Mvc.HandleErrorInfo

@{
    ViewBag.Title = "错误";
}

<hgroup class="title">
    <h1 class="error">错误。</h1>
    <h2 class="error">@((ViewData["ErrorMessage"] == null || ViewData["ErrorMessage"] == "") ? "处理你的请求出错" : ViewData["ErrorMessage"])</h2>
</hgroup>
